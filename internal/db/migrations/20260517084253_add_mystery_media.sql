-- +goose Up

CREATE TABLE mystery_media (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    mystery_id UUID NOT NULL REFERENCES mysteries(id) ON DELETE CASCADE,
    media_url TEXT NOT NULL,
    media_type TEXT NOT NULL,
    thumbnail_url TEXT NOT NULL DEFAULT '',
    sort_order INTEGER NOT NULL DEFAULT 0
);
CREATE INDEX idx_mystery_media_mystery_id ON mystery_media(mystery_id);

-- +goose Down

DROP INDEX IF EXISTS idx_mystery_media_mystery_id;
DROP TABLE IF EXISTS mystery_media;
